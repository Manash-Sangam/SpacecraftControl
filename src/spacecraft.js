class Spacecraft {
    constructor(x, y, z, direction) {//Spacecraft Structure
        this.x = x;
        this.y = y;
        this.z = z;
        this.direction = direction;
    }
}

module.exports = Spacecraft;